{
  "name": "convertor_be",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "PORT=3001 HOST=localhost DATABASE_URL=postgres://user1:pass@localhost:35432/db node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "node ./scripts/migrate.js",
    "migrate-dev": "DATABASE_URL=postgres://user1:pass@localhost:35432/db node ./scripts/migrate.js"
  },
  "author": "Xardas",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "pg": "^8.10.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.31.1"
  }
}
